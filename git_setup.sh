# set up git branches locally
git checkout -t origin/dimino_fixes dimino_fixes
#git branch -t origin/HEAD         
git checkout -t origin/new_perm_algo_SJT            
git checkout -t origin/tables_gfx 
git checkout -t origin/dimino_fixes_and_morphisms   
git checkout -t origin/isomorphism_invariants       
git checkout -t origin/revert_order_elements        
git checkout -t origin/distributed_solver           
git checkout -t origin/solve_cube                   
git checkout -t origin/galois_theory                
git checkout -t origin/mathshell                    
git checkout -t origin/special_polynomials          
git checkout master
